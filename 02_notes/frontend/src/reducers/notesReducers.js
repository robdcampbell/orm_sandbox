export const noteListReducer = (state = { notes: [] }, action) => {};

export const noteAddReducer = (state = { notes: [] }, action) => {};

export const noteUpdateReducer = (state = { notes: [] }, action) => {};

export const noteDeleteReducer = (state = { notes: [] }, action) => {};
